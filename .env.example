# Supabase (Postgres + Auth + Storage) — set up in Build Order step 1
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Geocoding — restaurant lat/lng at ingestion time (step 9, scraper)
GEOCODING_API_KEY=

# Image moderation — proactive photo scanning (step 11)
IMAGE_MODERATION_API_KEY=
